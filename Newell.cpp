#include "Newell.h"

void calcNormal(){
	Point p;
	/*int i,j;
	for (i=0, j = 1; i<faceVtxCount; i++, j++)
	{
		if (j == faceVtxCount) j = 0;
		p.x += (((vertexArray[faceVertexIndx[i]].z) + (vertexArray[faceVertexIndx[j]].z)) *
			((vertexArray[faceVertexIndx[j]].y) - (vertexArray[faceVertexIndx[i]].y)));
		p.y += (((vertexArray[faceVertexIndx[i]].x) + (vertexArray[faceVertexIndx[j]].x)) *
			((vertexArray[faceVertexIndx[j]].z) - (vertexArray[faceVertexIndx[i]].z)));
		p.z += (((vertexArray[faceVertexIndx[i]].y) + (vertexArray[faceVertexIndx[j]].y)) *
			((vertexArray[faceVertexIndx[j]].x) - (vertexArray[faceVertexIndx[i]].x)));
	}*/
}